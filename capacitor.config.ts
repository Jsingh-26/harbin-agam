import type { CapacitorConfig } from '@capacitor/cli'
const config: CapacitorConfig = { appId: 'com.jsingh26.harbinagam', appName: 'Harbin & Agam', webDir: 'dist', bundledWebRuntime: false, android: { backgroundColor: '#102342' } }
export default config
