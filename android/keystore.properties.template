# Copy this file to android/keystore.properties (same folder) and fill in the
# production signing details. android/keystore.properties and the keystore
# itself are gitignored - never commit them.
storeFile=harbin-agam-release.keystore
storePassword=
keyAlias=harbin-agam
keyPassword=
